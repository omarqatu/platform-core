{
  "_comment": "The approved privilege surface (PLATFORM_CORE v1.14 §3.8 matrix; §3.6 Check 6). Check 6 compares the catalog against this file in both directions: anything missing or extra is a failure. Only the application roles and PUBLIC are compared; migrator is the owner.",
  "roles": ["app_user", "authenticator", "job_runner", "provisioner"],
  "tables": {
    "tenants":                   { "app_user": ["SELECT"], "provisioner": ["INSERT"], "job_runner": ["SELECT"] },
    "persons":                   { "app_user": ["SELECT"], "provisioner": ["SELECT", "INSERT"] },
    "users":                     { "app_user": ["SELECT"], "authenticator": ["SELECT"], "provisioner": ["SELECT", "INSERT"] },
    "user_password_credentials": { "authenticator": ["SELECT"], "provisioner": ["INSERT"] },
    "memberships":               { "app_user": ["SELECT"], "provisioner": ["SELECT", "INSERT"] },
    "membership_roles":          { "app_user": ["SELECT", "INSERT", "DELETE"], "provisioner": ["INSERT"] },
    "membership_auth":           { "app_user": ["SELECT"], "authenticator": ["SELECT"], "provisioner": ["INSERT"] },
    "membership_scope":          { "app_user": ["SELECT"], "provisioner": ["INSERT"] },
    "scope_assignments":         { "app_user": ["SELECT", "INSERT"] },
    "invitations":               { "app_user": ["SELECT", "INSERT"], "provisioner": ["SELECT"] },
    "auth_attempts":             { "authenticator": ["SELECT", "INSERT"] },
    "audit_log":                 { "app_user": ["SELECT", "INSERT"], "provisioner": ["INSERT"] },
    "roles":                     { "app_user": ["SELECT", "INSERT", "UPDATE", "DELETE"], "provisioner": ["SELECT", "INSERT"] },
    "role_permissions":          { "app_user": ["SELECT", "INSERT", "DELETE"], "provisioner": ["INSERT"] },
    "tenant_modules":            { "app_user": ["SELECT"], "provisioner": ["INSERT"] },
    "modules":                   { "app_user": ["SELECT"], "provisioner": ["SELECT"] },
    "permissions":               { "app_user": ["SELECT"], "provisioner": ["SELECT"] },
    "role_templates":            { "provisioner": ["SELECT"] },
    "role_template_permissions": { "provisioner": ["SELECT"] }
  },
  "columns": {
    "users":                     { "app_user": { "UPDATE": ["last_login_at", "language", "theme"] } },
    "user_password_credentials": { "app_user": { "UPDATE": ["password_hash", "updated_at"] } },
    "memberships":               { "app_user": { "UPDATE": ["status"] } },
    "membership_scope":          { "app_user": { "UPDATE": ["scope_mode"] } },
    "scope_assignments":         { "app_user": { "UPDATE": ["active"] } },
    "invitations":               { "app_user": { "UPDATE": ["status"] }, "provisioner": { "UPDATE": ["status"] } },
    "tenant_modules":            { "app_user": { "UPDATE": ["is_active"] } }
  },
  "database": { "CONNECT": ["app_user", "authenticator", "job_runner", "provisioner"], "TEMPORARY": [] },
  "schemas": { "public": { "USAGE": ["app_user", "authenticator", "job_runner", "provisioner"], "CREATE": [] } },
  "executable_routines": [],
  "sequences": [],
  "views": [],
  "default_privileges": []
}
