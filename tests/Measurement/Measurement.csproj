<Project Sdk="Microsoft.NET.Sdk">

  <!-- PROOF_SPEC v1.3 T8 (spec item j): the measurement of the second axis's cost on the actual stack. Run by hand
       against a migrated database; not a CI step. See docs/measurements/README.md. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Modules.Subscriptions\Modules.Subscriptions.csproj" />
  </ItemGroup>

</Project>
